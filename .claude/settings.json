{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "node .claude/hooks/skill-sync-hint.js"
          },
          {
            "type": "command",
            "command": "bash -c 'file=\"$CLAUDE_TOOL_INPUT_FILE_PATH\"; if [[ \"$file\" == */api/*.js || \"$file\" == */src/*.js || \"$file\" == */tests/*.js ]]; then npx vitest related \"$file\" --run --passWithNoTests --reporter=dot 2>&1 | tail -15; fi; exit 0'"
          }
        ]
      }
    ],
    "Stop": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "npx vitest run --reporter=dot 2>&1 | tail -10"
          }
        ]
      }
    ]
  }
}
